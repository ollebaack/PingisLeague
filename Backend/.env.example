## IMPORTANT: Replace with your n8n webhook or execution endpoint.
## If using a Webhook Trigger node, use one of:
##   Test URL:  http://localhost:5678/webhook-test/<path>
##   Prod URL:  http://localhost:5678/webhook/<path>
## If calling the n8n REST API to run a workflow directly:
##   POST http://localhost:5678/rest/workflows/<id>/run  (requires API key)
## For now it's set to the UI workflow URL (will NOT execute). Change this.
N8N_WEBHOOK_URL=http://localhost:5678/webhook/<your-webhook-path>
PORT=4000